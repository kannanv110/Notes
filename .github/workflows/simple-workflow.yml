name: Simple workflow

on:
    push:
        branches:
            - main
    workflow_dispatch:
jobs:
    simple_workflow:
        runs-on: [self-hosted, gh-runner]
        steps:
            - name: Just print message
              run: echo "Hello World!"
